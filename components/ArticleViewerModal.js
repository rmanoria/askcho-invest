"use client";

// News articles are opened in a new browser tab instead of being embedded via iframe,
// because many publishers block framing and that behavior is outside our control.
export default function ArticleViewerModal() {
  return null;
}
